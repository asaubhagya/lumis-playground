import {fail,openai,requestBody} from '@/lib/server';
import {levels,scienceContract} from '@/lib/learning';
export async function POST(r:Request){try{const d=await requestBody(r,60000);if(typeof d.sdp!=='string'||!d.sdp.startsWith('v=0')||!Number.isInteger(d.level)||d.level<0||d.level>3)return Response.json({error:'Invalid voice session.'},{status:400});const upstream=await openai('live/sessions',{session:{model:'gpt-live-1',instructions:scienceContract+` You are a warm, concise science guide for a child aged ${d.age==='5–7'?'5 to 7':'8 to 10'}. Current challenge: ${levels[d.level].question}. Speak one short sentence at a time. Listen to interruptions. Delegate scientific reasoning and hints. The application can select a material, run an experiment, or open the discovery map when the learner explicitly asks. Delegate these commands; wait for the application result before saying they happened. Never submit predictions or unlock levels for the learner. Ask the child to tap or draw their own prediction.`,delegation:{type:'client'}},transport:{type:'webrtc',sdp:d.sdp}});return Response.json(await upstream.json());}catch(e){return fail(e);}}
